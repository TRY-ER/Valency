[project]
name = "mcp-setup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chembl-webresource-client",
    "mcp[cli]>=1.9.0",
    "rcsbsearchapi>=2.0.1",
]

[tool.uv.workspace]
members = [
    "mcp-setup",
]
